# Publishing @braintied/research is owned by braintied/stack.
# This repo is a source snapshot. Do not npm publish from here.
name: Snapshot
on:
  workflow_dispatch:
jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Publish from braintied/stack. This repo is a snapshot."
